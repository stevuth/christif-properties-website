
'use server';

// This file is intentionally left blank as image uploads are now handled
// on the client-side via Cloudinary.
// The presence of at least one server action file is required by Next.js
// in some configurations, so we keep this file.
export async function placeholderAction() {
  // Do nothing.
}
